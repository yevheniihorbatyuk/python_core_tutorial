# VSCode: Налаштування для Python розробки

## 🎯 Мета

> Практичний гайд для навчання і щоденної роботи: швидкий старт + стабільні налаштування для запуску, дебагу, форматування та базової продуктивності.

## Контекст Module 2 (canonical)
- Вхідна точка модуля: [`README.md`](README.md)
- Траєкторії навчання: [`docs/LEARNING_PATHS.md`](docs/LEARNING_PATHS.md)
- Канонічний flow заняття: [`docs/TEACHING_FLOW.md`](docs/TEACHING_FLOW.md)
- Baseline для середовища: **Python 3.10-3.12**

---

## 1. Встановлення VSCode

### Завантаження
- Перейдіть на [code.visualstudio.com](https://code.visualstudio.com)
- Завантажте версію для вашої ОС
- Встановіть програму

---

## 2. Як відкрити папку в VSCode

### Через меню
1. Відкрийте VSCode
2. `File` -> `Open Folder...`
3. Виберіть папку проєкту
4. Натисніть `Select Folder` / `Open`

### Через командний рядок
```bash
# Поточна папка
code .

# Конкретна папка
code /path/to/your/project
```

### Drag and drop
Перетягніть папку у вікно VSCode.

---

## 3. Обов'язкові розширення

### Python (Microsoft)
Дає запуск, debugging, базову інтеграцію Python у VSCode.

### Pylance (Microsoft)
Дає швидший IntelliSense, type checking, автоімпорти.

### Опціонально
- `Error Lens`
- `autoDocstring`
- `Material Icon Theme`

---

## 4. Базове налаштування

### Відкрити Settings
- `Ctrl+,` (Windows/Linux)
- `Cmd+,` (macOS)

### Рекомендовані параметри
- `Editor: Font Size = 14`
- `Files: Auto Save = afterDelay`
- `Editor: Format On Save = true`

### Formatter (узгоджений варіант)
Рекомендовано використовувати `black`.

```bash
python -m pip install black
# або
python3 -m pip install black
```

У VSCode:
- `Python: Formatting Provider` -> `black`
- `Editor: Default Formatter` -> `ms-python.black-formatter` (якщо встановлено)

---

## 5. Python interpreter і venv

### Вибір інтерпретатора
1. `Ctrl+Shift+P` / `Cmd+Shift+P`
2. `Python: Select Interpreter`
3. Оберіть Python 3.10-3.12 або `.venv`

### Рекомендований локальний venv
```bash
python -m venv .venv
# або
python3 -m venv .venv
```

Після створення знову оберіть `.venv` через `Python: Select Interpreter`.

---

## 6. Запуск Python файлу

### Через кнопку Run
1. Відкрийте `.py`
2. Натисніть `Run Python File`

### Через terminal
```bash
python homework_01.py
# або
python3 homework_01.py
```

---

## 7. Debugging (базово)

### Breakpoint
- Клік ліворуч від номера рядка
- Або `F9`

### Запуск debug
- `F5`
- Виберіть `Python File`

### Основні кроки
- `F10` Step Over
- `F11` Step Into
- `Shift+F11` Step Out
- `Shift+F5` Stop

Детальна практика: `06_debugging.py`.

---

## 8. Корисні гарячі клавіші

### Основні
- `Ctrl+S` / `Cmd+S` - зберегти
- `Ctrl+P` / `Cmd+P` - пошук файлу
- `Ctrl+Shift+P` / `Cmd+Shift+P` - Command Palette
- `Ctrl+/` / `Cmd+/` - коментар рядка

### Пошук
- `Ctrl+F` / `Cmd+F` - пошук у файлі
- `Ctrl+Shift+F` / `Cmd+Shift+F` - пошук у проєкті

---

## 9. Типові проблеми

### Python не знайдено
- Перевірте встановлення Python
- Перевиберіть інтерпретатор (`Python: Select Interpreter`)

### Немає автодоповнення
- Перевірте, що встановлено `Pylance`
- Перезапустіть VSCode

### Форматування не спрацьовує
- Перевірте, що встановлено `black`
- Перевірте `Editor: Format On Save` і `Default Formatter`

---

## 10. Практичний чекліст

1. Встановити VSCode
2. Встановити `Python` + `Pylance`
3. Відкрити проєкт у VSCode
4. Створити `test.py`
5. Запустити файл
6. Поставити breakpoint і пройти 2-3 кроки debug
7. Увімкнути `format on save` і перевірити форматування
