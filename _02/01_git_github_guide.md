# Git/GitHub: короткий гайд для Module 2

## 🎯 Мета
Навчитися базовому workflow для здачі ДЗ: `clone -> add -> commit -> push`.

## Контекст Module 2 (canonical)
- Вхідна точка модуля: [`README.md`](README.md)
- Траєкторії: [`docs/LEARNING_PATHS.md`](docs/LEARNING_PATHS.md)
- Flow заняття: [`docs/TEACHING_FLOW.md`](docs/TEACHING_FLOW.md)

---

## 1. Мінімальне налаштування Git (1 раз на ПК)

```bash
git config --global user.name "Ваше Ім'я"
git config --global user.email "your_email@example.com"
```

Перевірка:
```bash
git --version
git config --global --list
```

---

## 2. Доступ до GitHub: SSH або HTTPS+PAT

### Варіант A: SSH (рекомендовано для постійної роботи)

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub
```

Далі:
1. GitHub -> Settings -> SSH and GPG keys -> New SSH key
2. Вставити публічний ключ
3. Перевірити:

```bash
ssh -T git@github.com
```

### Варіант B: HTTPS + PAT
1. Створити Personal Access Token у GitHub.
2. При `git push` використовувати PAT як пароль.

---

## 3. Створення репозиторію на GitHub

1. `+` -> `New repository`
2. Назва: наприклад `python-course-module2`
3. Додати `README` і `.gitignore (Python)`
4. `Create repository`

---

## 4. Клонування репозиторію

```bash
# SSH
git clone git@github.com:username/python-course-module2.git

# або HTTPS
git clone https://github.com/username/python-course-module2.git

cd python-course-module2
```

---

## 5. Базовий workflow для ДЗ

```bash
# 1) Перевірити статус
git status

# 2) Додати зміни
git add .

# 3) Створити коміт
git commit -m "Додано рішення: завдання 1"

# 4) Відправити на GitHub
git push origin main
```

Рекомендації для commit message:
- коротко і по суті
- дієслово + що змінено
- приклад: `Виправлено перевірку валідності email`

---

## 6. Щоденний цикл роботи

```bash
# перед початком
# (у папці репозиторію)
git pull origin main

# робота над кодом
# ...

git add .
git commit -m "Оновлено рішення практичного блоку"
git push origin main
```

---

## 7. Корисні команди

```bash
git log --oneline
git diff
git remote -v
```

---

## 8. Типові проблеми

### `Permission denied (publickey)`
- SSH-ключ не додано в GitHub або не додано в `ssh-agent`

### `fatal: not a git repository`
- Ви не в папці репозиторію (`cd` у правильну директорію)

### `rejected` при `push`
- Локальна гілка відстає: зробіть `git pull origin main`, вирішіть конфлікти, потім `push`

---

## 9. Чекліст перед здачею

- [ ] Налаштовано Git user.name/user.email
- [ ] Є доступ до GitHub (SSH або HTTPS+PAT)
- [ ] Репозиторій створено і клоновано
- [ ] Зміни закомічені з осмисленим повідомленням
- [ ] `git push` виконано без помилок
- [ ] Файли видно на GitHub

---

## 10. Безпечні межі для beginner рівня

- Не використовувати destructive команди без розуміння (`reset --hard`, force push)
- Перед risky-діями перевіряти `git status`
- Якщо помилка незрозуміла, спочатку зберегти зміни і звернутися за допомогою
