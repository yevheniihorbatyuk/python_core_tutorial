"""
Модуль 2.4: Debugging (Налагодження) в VSCode
==============================================

Цей модуль показує як використовувати debugger для пошуку та виправлення помилок
"""

# ============================================================================
# ЩО ТАКЕ DEBUGGING?
# ============================================================================

"""
Debugging (налагодження) - процес знаходження та виправлення помилок у коді.

Debugger - інструмент, який дозволяє:
- Зупиняти виконання програми в певних місцях
- Переглядати значення змінних
- Виконувати код крок за кроком
- Розуміти як працює програма

ВАЖЛИВІСТЬ:
✅ Швидше знаходити помилки
✅ Розуміти логіку виконання
✅ Вчитися на помилках
✅ Бачити зміни змінних у реальному часі
"""

print("=" * 60)
print("ПРИКЛАДИ ДЛЯ DEBUGGING")
print("=" * 60)


# ============================================================================
# ПРИКЛАД 1: ПРОСТА ФУНКЦІЯ ДЛЯ DEBUGGING
# ============================================================================

def calculate_sum(numbers):
    """
    Обчислює суму чисел у списку
    
    ЗАВДАННЯ ДЛЯ DEBUGGING:
    1. Поставте breakpoint на рядку "total = 0"
    2. Запустіть debugger (F5)
    3. Дивіться як змінюється total у кожній ітерації
    """
    total = 0  # 👈 BREAKPOINT ТУТ
    
    for number in numbers:
        total = total + number  # Дивіться як змінюється total
        print(f"Поточна сума: {total}")
    
    return total


# Виклик функції
result = calculate_sum([5, 10, 15, 20])
print(f"Кінцевий результат: {result}")


# ============================================================================
# ПРИКЛАД 2: ПОШУК ЛІТЕРИ З DEBUGGING
# ============================================================================

def find_letter_positions(text, letter):
    """
    Знаходить всі позиції літери у тексті
    
    ЗАВДАННЯ ДЛЯ DEBUGGING:
    1. Breakpoint на "positions = []"
    2. Step Over (F10) через цикл
    3. Дивіться як змінюється i та positions
    4. Зверніть увагу коли виконується if
    """
    positions = []  # 👈 BREAKPOINT ТУТ
    
    for i in range(len(text)):
        char = text[i]
        print(f"Перевіряю позицію {i}: символ '{char}'")
        
        if char.lower() == letter.lower():
            positions.append(i)
            print(f"  ✅ Знайдено! Додаю {i} до списку")
    
    return positions


# Виклик
text = "Hello World"
letter = "o"
result = find_letter_positions(text, letter)
print(f"\nЛітера '{letter}' знайдена на позиціях: {result}")


# ============================================================================
# ПРИКЛАД 3: ПЛОЩА ТРИКУТНИКА
# ============================================================================

def calculate_triangle_area(base, height):
    """
    Обчислює площу трикутника за формулою: (основа * висота) / 2
    
    ЗАВДАННЯ ДЛЯ DEBUGGING:
    1. Breakpoint на першому рядку функції
    2. Перевірте значення параметрів base та height
    3. Step Over через обчислення
    4. Подивіться проміжний результат до ділення
    """
    print(f"Розраховую площу трикутника")
    print(f"Основа: {base}")
    print(f"Висота: {height}")
    
    # Обчислення
    product = base * height  # Подивіться це значення
    area = product / 2       # І це
    
    print(f"Площа: {area}")
    return area


# Тестування з різними значеннями
print("\n" + "=" * 60)
print("ТЕСТ 1:")
area1 = calculate_triangle_area(10, 5)
print(f"Результат: {area1}\n")

print("ТЕСТ 2:")
area2 = calculate_triangle_area(7.5, 4)
print(f"Результат: {area2}\n")


# ============================================================================
# ПРИКЛАД 4: ФУНКЦІЯ З ПОМИЛКОЮ (для практики debugging)
# ============================================================================

def calculate_average_buggy(numbers):
    """
    Ця функція має БАҐ!
    
    ЗАВДАННЯ:
    1. Використайте debugger щоб знайти помилку
    2. Подивіться на значення змінних
    3. Зрозумійте що йде не так
    """
    total = 0
    count = 0
    
    for num in numbers:
        total += num
        # count += 1  # 👈 БАҐ: ця лінія закоментована!
    
    average = total / count  # Що станеться якщо count = 0?
    return average


# Розкоментуйте для тестування:
# try:
#     result = calculate_average_buggy([5, 10, 15])
#     print(f"Середнє: {result}")
# except ZeroDivisionError as e:
#     print(f"❌ Помилка: {e}")
#     print("Використайте debugger щоб знайти проблему!")


# ============================================================================
# ПРИКЛАД 5: СКЛАДНІША ЛОГІКА
# ============================================================================

def find_max_and_min(numbers):
    """
    Знаходить максимальне та мінімальне число
    
    ЗАВДАННЯ ДЛЯ DEBUGGING:
    1. Breakpoint на початку функції
    2. Дивіться як змінюються max_num та min_num
    3. Зверніть увагу на умови if
    """
    if len(numbers) == 0:
        return None, None
    
    max_num = numbers[0]  # 👈 BREAKPOINT ТУТ
    min_num = numbers[0]
    
    for num in numbers[1:]:  # Починаємо з другого елемента
        print(f"Перевіряю число: {num}")
        
        if num > max_num:
            print(f"  {num} > {max_num} - оновлюю максимум")
            max_num = num
        
        if num < min_num:
            print(f"  {num} < {min_num} - оновлюю мінімум")
            min_num = num
    
    return max_num, min_num


# Тест
numbers = [15, 3, 42, 8, 23, 4, 16]
max_val, min_val = find_max_and_min(numbers)
print(f"\nМаксимум: {max_val}")
print(f"Мінімум: {min_val}")


# ============================================================================
# ПРИКЛАД 6: УМОВНА ЛОГІКА
# ============================================================================

def grade_calculator(score):
    """
    Визначає оцінку за балами
    
    ЗАВДАННЯ ДЛЯ DEBUGGING:
    1. Додайте breakpoint на початку
    2. Випробуйте з різними значеннями score
    3. Подивіться яка умова виконується
    """
    print(f"Оцінюю бал: {score}")
    
    if score >= 90:  # 👈 BREAKPOINT - дивіться умови
        grade = "Відмінно"
    elif score >= 75:
        grade = "Добре"
    elif score >= 60:
        grade = "Задовільно"
    else:
        grade = "Незадовільно"
    
    print(f"Оцінка: {grade}")
    return grade


# Тести
test_scores = [95, 80, 65, 45]
for score in test_scores:
    grade_calculator(score)
    print()


# ============================================================================
# ПРИКЛАД 7: РЕКУРСІЯ (ADVANCED)
# ============================================================================

def factorial(n):
    """
    Обчислює факторіал рекурсивно
    
    ЗАВДАННЯ ДЛЯ DEBUGGING:
    1. Breakpoint на першій лінії
    2. Step Into (F11) - увійдіть у рекурсивний виклик
    3. Дивіться Call Stack (стек викликів)
    4. Зверніть увагу як розгортається рекурсія
    """
    print(f"factorial({n}) викликано")
    
    if n <= 1:  # 👈 BREAKPOINT для рекурсії
        print(f"  База рекурсії: повертаю 1")
        return 1
    else:
        result = n * factorial(n - 1)
        print(f"  factorial({n}) = {n} * factorial({n-1}) = {result}")
        return result


# Тест рекурсії
print("\n" + "=" * 60)
print("РЕКУРСІЯ: ФАКТОРІАЛ")
print("=" * 60)
result = factorial(5)
print(f"\n5! = {result}")


# ============================================================================
# КОРИСНІ ТЕХНІКИ DEBUGGING
# ============================================================================

print("\n" + "=" * 60)
print("ТЕХНІКИ DEBUGGING")
print("=" * 60)

def debugging_techniques_demo():
    """Демонстрація різних технік"""
    
    # 1. Print statements (простий але ефективний спосіб)
    print("🔍 DEBUG: Початок функції")
    
    x = 10
    y = 20
    print(f"🔍 DEBUG: x={x}, y={y}")
    
    result = x + y
    print(f"🔍 DEBUG: result={result}")
    
    # 2. Використання assert для перевірки припущень
    assert result == 30, "Результат має бути 30"
    
    # 3. Try-except для обробки помилок
    try:
        division = 100 / 0
    except ZeroDivisionError as e:
        print(f"🔍 DEBUG: Спіймано помилку: {e}")
    
    return result


debugging_techniques_demo()


# ============================================================================
# ІНСТРУКЦІЯ З ВИКОРИСТАННЯ DEBUGGER
# ============================================================================

debug_instructions = """
╔══════════════════════════════════════════════════════════════╗
║              ІНСТРУКЦІЯ: ЯК ВИКОРИСТОВУВАТИ DEBUGGER         ║
╚══════════════════════════════════════════════════════════════╝

1️⃣ ДОДАТИ BREAKPOINT (точку зупинки):
   - Клікніть ліворуч від номера рядка
   - Або поставте курсор на рядок і натисніть F9
   - Червона крапка = активний breakpoint

2️⃣ ЗАПУСТИТИ DEBUGGER:
   - Натисніть F5
   - Або клікніть Run → Start Debugging
   - Виберіть "Python File"

3️⃣ ПАНЕЛЬ УПРАВЛІННЯ (Debug Toolbar):
   ▶️  Continue (F5)           - продовжити до наступного breakpoint
   ⏸️  Pause                   - призупинити виконання
   🔄  Step Over (F10)         - виконати рядок (не входячи в функції)
   ⬇️  Step Into (F11)         - увійти у функцію
   ⬆️  Step Out (Shift+F11)    - вийти з поточної функції
   🔄  Restart (Ctrl+Shift+F5) - перезапустити
   ⏹️  Stop (Shift+F5)         - зупинити

4️⃣ ЩО ДИВИТИСЯ:

   📊 VARIABLES (Змінні):
      - Ліва панель показує всі змінні та їх значення
      - Locals: локальні змінні
      - Globals: глобальні змінні
   
   📞 CALL STACK (Стек викликів):
      - Показує ланцюг викликів функцій
      - Корисно для рекурсії
   
   👁️ WATCH (Спостереження):
      - Додайте вирази для відстеження
      - Наприклад: len(text), x + y
   
   🔴 BREAKPOINTS:
      - Список всіх breakpoints
      - Можна тимчасово вимкнути

5️⃣ ПОРАДИ:

   ✅ Починайте з простих прикладів
   ✅ Використовуйте Step Over (F10) для перегляду покроково
   ✅ Дивіться на зміни змінних
   ✅ Step Into (F11) для входу в функції
   ✅ Додавайте вирази в Watch для відстеження
   ✅ Порівнюйте очікувані та реальні значення

6️⃣ ТИПОВІ ЗАВДАННЯ:

   🔍 Знайти помилку:
      1. Додайте breakpoint де починається проблема
      2. Запустіть debugger
      3. Дивіться на значення змінних
      4. Крок за кроком знайдіть де значення стає неправильним
   
   🔍 Зрозуміти логіку:
      1. Breakpoint на початку функції
      2. Step Over (F10) через кожен рядок
      3. Дивіться як змінюються змінні
      4. Зверніть увагу на умови if/else

7️⃣ ПРАКТИКА:

   Для кожного прикладу у цьому файлі:
   ✓ Додайте breakpoint де вказано 👈
   ✓ Запустіть debugger
   ✓ Пройдіть крок за кроком
   ✓ Дивіться на змінні
   ✓ Зрозумійте логіку виконання
"""

print(debug_instructions)


# ============================================================================
# ПРАКТИЧНІ ЗАВДАННЯ
# ============================================================================

print("\n" + "=" * 60)
print("ЗАВДАННЯ ДЛЯ ПРАКТИКИ DEBUGGING")
print("=" * 60)

practice_tasks = """
1️⃣ БАЗОВИЙ РІВЕНЬ:
   - Запустіть calculate_sum([1, 2, 3, 4, 5]) з debugger
   - Дивіться як змінюється total на кожній ітерації
   - Передбачте наступне значення перед Step Over

2️⃣ СЕРЕДНІЙ РІВЕНЬ:
   - Запустіть find_letter_positions("Programming", "r")
   - Відстежуйте змінні i, char, positions
   - Зверніть увагу коли спрацьовує if

3️⃣ ПОШУК ПОМИЛКИ:
   - Розкоментуйте calculate_average_buggy
   - Знайдіть помилку за допомогою debugger
   - Виправте код

4️⃣ СКЛАДНА ЛОГІКА:
   - Запустіть find_max_and_min з debugger
   - Відстежуйте max_num та min_num
   - Зрозумійте коли вони оновлюються

5️⃣ РЕКУРСІЯ (ADVANCED):
   - Запустіть factorial(4) з debugger
   - Використайте Step Into (F11)
   - Подивіться на Call Stack
   - Зрозумійте як розгортається рекурсія

💡 ДОДАТКОВЕ ЗАВДАННЯ:
   Напишіть власну функцію з помилкою та знайдіть її за допомогою debugger!
"""

print(practice_tasks)


# ============================================================================
# ПІДСУМОК
# ============================================================================

print("\n" + "=" * 60)
print("ПІДСУМОК")
print("=" * 60)

summary = """
✅ Debugger - потужний інструмент для розуміння коду

✅ Основні дії:
   - F9: додати/видалити breakpoint
   - F5: запустити debugger / continue
   - F10: Step Over (наступний рядок)
   - F11: Step Into (увійти в функцію)
   - Shift+F11: Step Out (вийти з функції)

✅ Що дивитися:
   - Variables: значення змінних
   - Call Stack: ланцюг викликів
   - Watch: відстежувані вирази

✅ Коли використовувати:
   - Шукаєте помилку
   - Не розумієте логіку
   - Вчите новий код
   - Перевіряєте гіпотезу

✅ Переваги debugger vs print():
   - Інтерактивність
   - Бачите всі змінні
   - Не треба додавати/видаляти print
   - Крок за кроком виконання

🎯 ГОЛОВНА ПОРАДА:
   Практикуйтесь! Чим більше користуєтесь debugger,
   тим швидше знаходите помилки!
"""

print(summary)

print("\n✨ Тепер ви знаєте як дебажити код! Практикуйтесь! ✨\n")
