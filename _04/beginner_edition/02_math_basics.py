"""
Модуль 4.2: Математика та Випадкові Числа - Beginner Edition
==============================================================

Це простий модуль для навчання основним математичним операціям у Python.
Включає приклади з:
- Базова арифметика та операції
- Роботи з числами (округлення, абсолютне значення)
- Генерування випадкових чисел
- Простих математичних функцій
- Практичних завдань

Це версія для новачків - простага, з багато коментарів та поясненнями.
"""

import random
import math

print("=" * 70)
print("МОДУЛЬ 4.2: МАТЕМАТИКА ТА ВИПАДКОВІ ЧИСЛА - BEGINNER EDITION")
print("=" * 70)

# ============================================================================
# 1. БАЗОВА АРИФМЕТИКА
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 1: БАЗОВА АРИФМЕТИКА")
print("=" * 70)

print("\n1. ОСНОВНІ ОПЕРАЦІЇ:")
print("-" * 70)

# Додавання
a = 10
b = 3
sum_result = a + b
print(f"{a} + {b} = {sum_result}")

# Віднімання
difference = a - b
print(f"{a} - {b} = {difference}")

# Множення
product = a * b
print(f"{a} * {b} = {product}")

# Ділення (повертає float)
quotient = a / b
print(f"{a} / {b} = {quotient:.2f}")

# Ціле ділення (без дробової частини)
int_division = a // b
print(f"{a} // {b} = {int_division}")

# Остача від ділення (модуль)
remainder = a % b
print(f"{a} % {b} = {remainder}")

# Степінь
power = a ** b
print(f"{a} ** {b} = {power}")

print()

print("\n2. ПОРЯДОК ОПЕРАЦІЙ:")
print("-" * 70)

# У Python використовується той же порядок як у математиці
# 1. Дужки ()
# 2. Степені **
# 3. Множення та ділення * /
# 4. Додавання та віднімання + -

result1 = 2 + 3 * 4
print(f"2 + 3 * 4 = {result1}  (множення першим, потім додавання)")

result2 = (2 + 3) * 4
print(f"(2 + 3) * 4 = {result2}  (дужки змінюють порядок)")

result3 = 10 - 5 + 2
print(f"10 - 5 + 2 = {result3}  (зліва направо)")

print()

# ============================================================================
# 2. РОБОТИ З ЧИСЛАМИ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 2: РОБОТИ З ЧИСЛАМИ")
print("=" * 70)

print("\n1. ОКРУГЛЕННЯ:")
print("-" * 70)

num = 3.14159

# Округлення до найближчого цілого
rounded = round(num)
print(f"round({num}) = {rounded}")

# Округлення до певної кількості десяткових місць
rounded_2 = round(num, 2)
print(f"round({num}, 2) = {rounded_2}")

rounded_4 = round(num, 4)
print(f"round({num}, 4) = {rounded_4}")

print()

print("\n2. АБСОЛЮТНЕ ЗНАЧЕННЯ:")
print("-" * 70)

# Абсолютне значення - число без знака (завжди позитивне)
negative = -42
positive = 42

abs_negative = abs(negative)
abs_positive = abs(positive)

print(f"abs({negative}) = {abs_negative}")
print(f"abs({positive}) = {abs_positive}")

# Useful for finding distance
distance1 = 10
distance2 = 25
distance_diff = abs(distance1 - distance2)
print(f"\nDistance between {distance1} and {distance2}: {distance_diff}")

print()

print("\n3. МІНІМУМ ТА МАКСИМУМ:")
print("-" * 70)

numbers = [5, 2, 8, 1, 9, 3]
minimum = min(numbers)
maximum = max(numbers)

print(f"Числа: {numbers}")
print(f"Мінімум: {minimum}")
print(f"Максимум: {maximum}")

# Із 2 чисел
number1 = 15
number2 = 7
print(f"\nmin({number1}, {number2}) = {min(number1, number2)}")
print(f"max({number1}, {number2}) = {max(number1, number2)}")

print()

print("\n4. СТЕПЕНІ ТА КОРЕНІ:")
print("-" * 70)

# Степінь - множимо число саме на себе
number = 3
square = number ** 2
cube = number ** 3

print(f"{number}² = {number} * {number} = {square}")
print(f"{number}³ = {number} * {number} * {number} = {cube}")

# Квадратний корінь - зворотне від квадрату
number = 16
square_root = math.sqrt(number)
print(f"√{number} = {square_root}")

# Перевіримо: корінь в квадраті = оригінальне число
check = square_root ** 2
print(f"  Перевірка: {square_root}² = {check}")

print()

# ============================================================================
# 3. ГЕНЕРУВАННЯ ВИПАДКОВИХ ЧИСЕЛ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 3: ВИПАДКОВІ ЧИСЛА")
print("=" * 70)

print("\n1. ВИПАДКОВІ ЧИСЛА ВІД 0 ДО 1:")
print("-" * 70)

# random.random() дає числа від 0.0 до 1.0
print("П'ять випадкових чисел:")
for i in range(5):
    random_num = random.random()
    print(f"  {i+1}. {random_num:.4f}")

print()

print("\n2. ВИПАДКОВІ ЦІЛІ ЧИСЛА:")
print("-" * 70)

# Кидання монети (0 або 1)
print("Кидання монети 5 раз (0=Решка, 1=Орел):")
for i in range(5):
    coin = random.randint(0, 1)
    side = "Орел" if coin == 1 else "Решка"
    print(f"  {i+1}. {side}")

print()

# Кидання кубика
print("Кидання кубика 5 раз:")
for i in range(5):
    dice = random.randint(1, 6)
    print(f"  {i+1}. Випало {dice}")

print()

print("\n3. ВИБІР ІЗ СПИСКУ:")
print("-" * 70)

colors = ["Червоний", "Синій", "Зелений", "Жовтий"]
print(f"Список кольорів: {colors}")
print("\nВипадковий вибір 3 рази:")

for i in range(3):
    selected = random.choice(colors)
    print(f"  {i+1}. {selected}")

print()

# ============================================================================
# 4. ПРАКТИЧНІ ПРИКЛАДИ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 4: ПРАКТИЧНІ ПРИКЛАДИ")
print("=" * 70)

print("\n1. ОБЧИСЛЕННЯ ПЛОЩІ КРУГА:")
print("-" * 70)

# Формула: S = π * r²
# π (пі) = 3.14159... (є у модулі math)

radius = 5
area = math.pi * radius ** 2

print(f"Радіус круга: {radius} см")
print(f"Площа = π * r² = {math.pi:.4f} * {radius}² = {area:.2f} см²")

print()

print("\n2. ОБЧИСЛЕННЯ ВІДСОТКІВ:")
print("-" * 70)

# Скільки буде 20% від 150?
total = 150
percent = 20
result = (total * percent) / 100

print(f"{percent}% від {total} = {result}")

# Приклад: знижка
price = 100
discount = 15  # 15% знижка
new_price = price - (price * discount / 100)

print(f"\nОригінальна ціна: {price} грн")
print(f"Знижка: {discount}%")
print(f"Нова ціна: {new_price:.2f} грн")

print()

print("\n3. СЕРЕДНЄ ЗНАЧЕННЯ (AVERAGE):")
print("-" * 70)

grades = [85, 90, 78, 92, 88]
sum_grades = sum(grades)
count = len(grades)
average = sum_grades / count

print(f"Оцінки: {grades}")
print(f"Сума: {sum_grades}")
print(f"Кількість оцінок: {count}")
print(f"Середня оцінка: {average:.2f}")

print()

print("\n4. СТАТИСТИКА КУБИКА (ТЕСТ ВИПАДКОВОСТІ):")
print("-" * 70)

# Кидаємо кубик 600 раз і рахуємо результати
results = {}
rolls = 600

for _ in range(rolls):
    number = random.randint(1, 6)
    if number in results:
        results[number] += 1
    else:
        results[number] = 1

print(f"Кидання кубика {rolls} разів:")
print()

# Сортуємо по номеру (1, 2, 3, 4, 5, 6)
for number in sorted(results.keys()):
    count = results[number]
    percentage = (count / rolls) * 100
    bar = "█" * (count // 20)  # Графік з блоків
    print(f"  Число {number}: {count:3d} раз ({percentage:5.1f}%) {bar}")

print(f"\nОчікуємо ~{rolls/6:.0f} разів для кожного числа")
print(f"Наші результати близькі до очікуваних? Це показує що random.randint() справді випадковий")

print()

# ============================================================================
# 5. ФУНКЦІЇ MATH МОДУЛЯ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 5: MATH МОДУЛЬ - КОРИСНІ ФУНКЦІЇ")
print("=" * 70)

print("\n1. ЧИСЛОВІ КОНСТАНТИ:")
print("-" * 70)

print(f"π (Пі): {math.pi:.6f}")
print(f"e (Число Ейлера): {math.e:.6f}")

print()

print("\n2. КОРЕНІ ТА СТЕПЕНІ:")
print("-" * 70)

number = 16
print(f"math.sqrt({number}) = {math.sqrt(number)}")

number = 2
power_exp = 10
print(f"math.pow({number}, {power_exp}) = {math.pow(number, power_exp)}")

print()

print("\n3. ЛОГАРИФМИ (Для вивчених):")
print("-" * 70)

number = 10
log10 = math.log10(number)
print(f"math.log10({number}) = {log10}")

number = 2.718281828
ln = math.log(number)  # натуральний логарифм
print(f"math.log(e) = {ln:.2f}")

print()

print("\n4. ТРИГОНОМЕТРІЯ (Синус, Косинус) - Для вивчених:")
print("-" * 70)

# Важливо: функції math використовують радіани, не градуси
# 90 градусів = π/2 радіан
# 180 градусів = π радіан

angle_radians = math.pi / 2  # 90 градусів
sine = math.sin(angle_radians)
cosine = math.cos(angle_radians)

print(f"Кут: π/2 радіан (90 градусів)")
print(f"sin(π/2) = {sine:.2f}")
print(f"cos(π/2) = {cosine:.4f}")

print()

# ============================================================================
# 6. ПРАКТИЧНІ ЗАВДАННЯ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 6: ПРАКТИЧНІ ЗАВДАННЯ")
print("=" * 70)

print("""
ЗАВДАННЯ 1 (ЛЕГКО): Площа та периметр прямокутника
  - Дано: довжина = 5, ширина = 3
  - Знайти: площа (довжина * ширина) та периметр (2 * (довжина + ширина))

ЗАВДАННЯ 2 (ЛЕГКО): Оплата рахунку
  - Дано: рахунок = 150 грн, чайові = 15%
  - Знайти: сума чайових та фінальна сума

ЗАВДАННЯ 3 (СЕРЕДНЬО): Простота числа
  - Число просте якщо ділиться тільки на 1 та на себе
  - Перевірити чи число 17 просте
  - Підказка: пройдіть від 2 до √17 та перевірте остачу від ділення

ЗАВДАННЯ 4 (СЕРЕДНЬО): Лотерея
  - Створіть програму що генерує 6 випадкових чисел від 1 до 49
  - Числа повинні бути різні (без дублів)
  - Покажіть результат в порядку зростання

ЗАВДАННЯ 5 (СКЛАДНІШЕ): Нормалізація оцінок
  - Дано список оцінок: [45, 67, 89, 23, 95]
  - Знайти мін та макс
  - Нормалізувати кожну оцінку від 0 до 1 за формулою:
    (оцінка - мін) / (макс - мін)

Спробуйте розв'язати завдання перед дивиттям рішення!
""")

print("\n" + "=" * 70)
print("ВИСНОВКИ")
print("=" * 70)

print("""
✅ Що ви дізналися:
  1. Базова арифметика (+ - * / // % **)
  2. Округлення та абсолютне значення
  3. Мінімум, максимум, суму чисел
  4. Корені та степені
  5. Генерування випадкових чисел
  6. Корисні функції з math модуля

💡 Ключові моменти:
  - Python дотримується нормального порядку операцій
  - Діління (/) завжди дає float, (//) дає ціле число
  - random модуль дозволяє створювати випадкові числа
  - math модуль має вбудовані математичні функції

🚀 Наступне: Регулярні вирази для пошуку по текстам
""")
