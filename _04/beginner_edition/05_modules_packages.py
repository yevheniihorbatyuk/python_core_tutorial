"""
Модуль 4.5: Модулі та Пакети - Beginner Edition
================================================

Це простий модуль для навчання організації коду у Python.
Включає приклади з:
- Що таке модулі та як їх використовувати
- Built-in модулі (math, random, datetime, re)
- Імпорт функцій та класів
- Створення власних модулів
- Що таке пакети (packages)
- pip - менеджер пакетів
- Virtual environments (venv)

Модулі дозволяють організувати код та переиспользовувати функції.

Це версія для новачків - простага, з багато коментарів та поясненнями.
"""

import math
import random
import os
import sys

print("=" * 70)
print("МОДУЛЬ 4.5: МОДУЛІ ТА ПАКЕТИ - BEGINNER EDITION")
print("=" * 70)

# ============================================================================
# 1. ЩО ТАКЕ МОДУЛІ?
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 1: ЩО ТАКЕ МОДУЛІ?")
print("=" * 70)

print("""
Модуль - це Python файл з коду.
  - Інший файл: functions.py, utils.py, etc.
  - Містить функції, класи, змінні
  - Можна імпортувати та використовувати в іншому файлі

Можна використовувати готові модулі (built-in) або писати свої.

Приклади built-in модулів:
  - math: математичні функції
  - random: випадкові числа
  - datetime: дати та час
  - os: файлова система
  - sys: системні параметри
  - re: регулярні вирази
""")

# ============================================================================
# 2. BUILT-IN МОДУЛІ - MATH
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 2: BUILT-IN МОДУЛІ")
print("=" * 70)

print("\n1. МОДУЛЬ math:")
print("-" * 70)

# Коли ми написуємо 'import math', ми підключаємо модуль
# Тепер можемо використовувати функції з math:

number = 16
square_root = math.sqrt(number)
absolute_val = math.fabs(-42)
power = math.pow(2, 10)

print(f"math.sqrt({number}) = {square_root}")
print(f"math.fabs(-42) = {absolute_val}")
print(f"math.pow(2, 10) = {power}")

print(f"\nКонстанти math:")
print(f"  math.pi = {math.pi}")
print(f"  math.e = {math.e}")

print()

print("\n2. МОДУЛЬ random:")
print("-" * 70)

# random - для випадкових чисел

random_1_to_6 = random.randint(1, 6)
random_0_to_1 = random.random()
choice = random.choice(['Red', 'Green', 'Blue'])

print(f"random.randint(1, 6) = {random_1_to_6}")
print(f"random.random() = {random_0_to_1:.4f}")
print(f"random.choice(['Red', 'Green', 'Blue']) = {choice}")

print()

print("\n3. МОДУЛЬ os:")
print("-" * 70)

# os - для роботи з файловою системою та операційною системою

current_dir = os.getcwd()
is_file = os.path.isfile("05_modules_packages.py")

print(f"os.getcwd() = {current_dir}")
print(f"os.path.isfile('05_modules_packages.py') = {is_file}")

# Список файлів в поточній директорії
files = os.listdir('.')
print(f"Файли в поточній директорії: {len(files)} файлів")

print()

print("\n4. МОДУЛЬ sys:")
print("-" * 70)

# sys - системна інформація

python_version = sys.version
python_version_info = sys.version_info

print(f"Python версія: {python_version.split()[0]}")
print(f"Major: {python_version_info.major}")
print(f"Minor: {python_version_info.minor}")

# Шляхи до модулів
print(f"\nКількість шляхів до модулів: {len(sys.path)}")
print(f"Перший шлях: {sys.path[0]}")

print()

# ============================================================================
# 3. РІЗНІ СПОСОБИ ІМПОРТУ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 3: РІЗНІ СПОСОБИ ІМПОРТУ")
print("=" * 70)

print("\n1. import модуль:")
print("-" * 70)

# Уже зробили: import math
# Тепер використовуємо: math.sqrt()

print("import math")
print("math.sqrt(9) = math.sqrt(9)")
print(f"Результат: {math.sqrt(9)}")

print()

print("\n2. from модуль import функція:")
print("-" * 70)

# Імпортуємо конкретну функцію
from math import cos, sin

result_cos = cos(0)
result_sin = sin(0)

print("from math import cos, sin")
print(f"cos(0) = {result_cos}")
print(f"sin(0) = {result_sin}")

print()

print("\n3. from модуль import * (не рекомендується):")
print("-" * 70)

# Цей спосіб імпортує ВСЕ з модуля
# Недобре, бо: може привести до конфліктів імен

print("from math import *  # Імпортує все (не рекомендується)")
print("ceil(3.2) = ceil(3.2)  (спеціально не тестуємо, щоб уникнути конфліктів)")

print()

print("\n4. import модуль as ім'я (alias):")
print("-" * 70)

# Даємо модулю коротше ім'я
import datetime as dt

today = dt.datetime.now()
print(f"import datetime as dt")
print(f"Сьогодні: {today.strftime('%Y-%m-%d')}")

print()

# ============================================================================
# 4. СТВОРЕННЯ ВЛАСНОГО МОДУЛЯ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 4: СТВОРЕННЯ ВЛАСНОГО МОДУЛЯ")
print("=" * 70)

print("""
Щоб створити модуль:
  1. Напишіть функції в новому файлі (наприклад, my_functions.py)
  2. Зберегти файл
  3. Імпортувати в іншому файлі: from my_functions import функція

Приклад my_functions.py:
  def додати(a, b):
      return a + b

  def помножити(a, b):
      return a * b

Тепер можна імпортувати:
  from my_functions import додати
  результат = додати(5, 3)
""")

# Створимо простий модуль для демонстрації
module_code = '''"""
Простий модуль з корисними функціями.
"""

def додати(a, b):
    """Додає два числа."""
    return a + b

def помножити(a, b):
    """Множить два числа."""
    return a * b

def коли_квадрат(n):
    """Повертає квадрат числа."""
    return n ** 2

# Константа
ПІ = 3.14159
'''

# Запишемо цей модуль у файл
with open('simple_module.py', 'w', encoding='utf-8') as f:
    f.write(module_code)

print("\n1. СТВОРЕНИЙ МОДУЛЬ:")
print("-" * 70)

print("✅ Файл simple_module.py створено")
print("\nВміст:")
print(module_code)

print()

print("\n2. ВИКОРИСТАННЯ ВАШОГО МОДУЛЯ:")
print("-" * 70)

# Тепер імпортуємо нашу функцію
from simple_module import додати, помножити, коли_квадрат, ПІ

result1 = додати(10, 5)
result2 = помножити(4, 7)
result3 = коли_квадрат(5)

print(f"from simple_module import додати, помножити, коли_квадрат, ПІ")
print()
print(f"додати(10, 5) = {result1}")
print(f"помножити(4, 7) = {result2}")
print(f"коли_квадрат(5) = {result3}")
print(f"ПІ = {ПІ}")

print()

# ============================================================================
# 5. ШО ТАКЕ ПАКЕТИ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 5: ШО ТАКЕ ПАКЕТИ (PACKAGES)")
print("=" * 70)

print("""
Пакет - це директорія з модулями.

Структура пакета:
  my_package/
    __init__.py      # Робить це пакетом
    module1.py       # Модуль 1
    module2.py       # Модуль 2
    sub_package/     # Піддиректорія (тоже пакет)
      __init__.py
      module3.py

Файл __init__.py важливий - він робить директорію пакетом.
Може бути порожнім або містити ініціалізаційний код.

Використання:
  from my_package.module1 import функція
  from my_package.sub_package.module3 import клас
""")

# Створимо простий пакет
os.makedirs('my_package', exist_ok=True)

with open('my_package/__init__.py', 'w', encoding='utf-8') as f:
    f.write('# Це пакет my_package\n')

with open('my_package/helpers.py', 'w', encoding='utf-8') as f:
    f.write('''"""Допоміжні функції."""

def привіт(ім_я):
    return f"Привіт, {ім_я}!"

def площа_круга(радіус):
    import math
    return math.pi * радіус ** 2
''')

print("\n1. СТВОРЕНИЙ ПАКЕТ:")
print("-" * 70)

print("✅ Пакет my_package/ створено")
print("  my_package/")
print("    __init__.py")
print("    helpers.py")

print()

print("\n2. ВИКОРИСТАННЯ МОДУЛЯ З ПАКЕТА:")
print("-" * 70)

from my_package.helpers import привіт, площа_круга

greeting_result = привіт("Іване")
area_result = площа_круга(5)

print(f"from my_package.helpers import привіт, площа_круга")
print()
print(f"привіт('Іване') = {greeting_result}")
print(f"площа_круга(5) = {area_result:.2f}")

print()

# ============================================================================
# 6. pip - МЕНЕДЖЕР ПАКЕТІВ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 6: PIP - МЕНЕДЖЕР ПАКЕТІВ")
print("=" * 70)

print("""
pip - це менеджер пакетів Python.
Дозволяє встановлювати пакети з PyPI (Python Package Index).

Команди pip:
  pip install пакет           - встановити пакет
  pip install пакет==1.0      - встановити конкретну версію
  pip list                     - список встановлених пакетів
  pip uninstall пакет         - видалити пакет
  pip freeze > requirements.txt - список залежностей

Приклади популярних пакетів:
  - requests: HTTP запити
  - numpy: чисельні обчислення
  - pandas: робота з даними
  - django: веб-фреймворк
  - flask: легкий веб-фреймворк
  - matplotlib: графіки

Приклад встановлення:
  pip install requests
  pip install numpy pandas
""")

# Покажемо встановлені пакети
import subprocess

print("\n1. ВСТАНОВЛЕНІ ПАКЕТИ (перші 10):")
print("-" * 70)

try:
    result = subprocess.run(['pip', 'list'], capture_output=True, text=True)
    packages = result.stdout.split('\n')[:12]  # Перші 12 рядків
    for line in packages:
        print(line)
except Exception as e:
    print(f"(Не вдається показати список: {e})")

print()

# ============================================================================
# 7. VIRTUAL ENVIRONMENTS
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 7: VIRTUAL ENVIRONMENTS (venv)")
print("=" * 70)

print("""
Virtual environment - це ізольоване окремо Python оточення.

Чому це потрібно?
  - Різні проекти можуть потребувати різних версій пакетів
  - venv ізолює залежності проекту
  - Не забруднює глобальне Python оточення

Команди:
  python3 -m venv venv              - створити venv
  source venv/bin/activate          - активувати (Linux/Mac)
  venv\\Scripts\\activate            - активувати (Windows)
  deactivate                         - деактивувати
  pip freeze > requirements.txt      - експортувати залежності
  pip install -r requirements.txt    - встановити залежності

Приклад workflow:
  1. python3 -m venv my_venv
  2. source my_venv/bin/activate
  3. pip install requests numpy
  4. ... розроблювати проект ...
  5. deactivate
""")

print("⚠️  Virtual environment не показуємо в дії (це вимагає shell команд)")

print()

# ============================================================================
# 8. ПРАКТИЧНІ ПРИКЛАДИ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 8: ПРАКТИЧНІ ПРИКЛАДИ")
print("=" * 70)

print("\n1. ВИКОРИСТАННЯ MULTIPLE MODULES:")
print("-" * 70)

# Комбінуємо декілька модулів
import math
import random

radius = random.randint(1, 10)
area = math.pi * (radius ** 2)

print(f"Випадковий радіус: {radius}")
print(f"Площа круга: {area:.2f}")

print()

print("\n2. МОДУЛЬНА СТРУКТУРА ПРОЕКТУ:")
print("-" * 70)

print("""
Приклад організації великого проекту:

my_project/
  main.py              # Головна програма
  config.py            # Конфігурація
  utils/
    __init__.py
    helpers.py         # Допоміжні функції
    validators.py      # Валідація
  models/
    __init__.py
    user.py            # Модель користувача
    product.py         # Модель продукту
  data/
    input.csv
    output.json

Тепер можна імпортувати:
  from utils.helpers import функція
  from models.user import User
""")

print()

# ============================================================================
# 9. ЗАВДАННЯ ДЛЯ ПРАКТИКИ
# ============================================================================

print("\n" + "=" * 70)
print("ЧАСТИНА 9: ПРАКТИЧНІ ЗАВДАННЯ")
print("=" * 70)

print("""
ЗАВДАННЯ 1 (ЛЕГКО): Використання модуля math
  - Імпортуйте функції з math
  - Обчисліть: sqrt(100), pow(2, 8), factorial(5)
  - Виведіть результати

ЗАВДАННЯ 2 (ЛЕГКО): Створення простого модуля
  - Створіть файл calculator.py з функціями: add, subtract, multiply
  - Імпортуйте в цей скрипт та протестуйте

ЗАВДАННЯ 3 (СЕРЕДНЬО): Работа з os модулем
  - Виведіть поточну директорію
  - Список всіх файлів в поточній директорії
  - Перевірте чи це файл або папка для кожного

ЗАВДАННЯ 4 (СЕРЕДНЬО): Створення пакета
  - Створіть структуру: my_tools/
  - Додайте два модулі: math_tools.py та string_tools.py
  - Імпортуйте та використовуйте функції

ЗАВДАННЯ 5 (СКЛАДНО): requirements.txt та venv
  - Напишіть requirements.txt з пакетами які хочете встановити
  - Документуйте як створити venv та встановити залежності
  - Наведіть приклад використання 3-4 популярних пакетів

Розв'яжіть завдання перед дивіттям рішення!
""")

print("\n" + "=" * 70)
print("ВИСНОВКИ")
print("=" * 70)

print("""
✅ Що ви дізналися:
  1. Модулі - це Python файли з кодом
  2. Built-in модулі (math, random, os, sys, etc.)
  3. Різні способи імпорту (import, from import, as)
  4. Створення власних модулів
  5. Пакети - директорії з модулями
  6. pip - менеджер пакетів для встановлення пакетів
  7. Virtual environments для ізоляції залежностей

💡 Ключові моменти:
  - Використовуйте модулі для організації коду
  - built-in модулі завжди доступні (math, os, random)
  - pip install пакет для встановлення нових пакетів
  - Virtual environment для ізоляції проекту
  - __init__.py робить директорію пакетом

⚠️  Поради для новачків:
  - Не забувайте import перед використанням функцій модуля
  - Наведіть з PyPI офіційну документацію пакета перед встановленням
  - Завжди використовуйте venv для нових проектів
  - Експортуйте залежності з pip freeze > requirements.txt

🚀 Наступне: Практичні проекти - примени це все разом
""")

# Очистка: видалимо тестові файли
import shutil

if os.path.exists('simple_module.py'):
    os.remove('simple_module.py')

if os.path.exists('my_package'):
    shutil.rmtree('my_package')

print("\n(Тестові файли очищено)")
