"""
Модуль 4.5: Модулі та пакети в Python
=======================================

Цей модуль показує:
- Що таке модулі та як їх імпортувати
- Створення власних модулів
- Пакети та файл __init__.py
- pip та віртуальне оточення (venv)
- Модулі math, random, os, sys, pathlib
"""

import math
import sys
import os
from pathlib import Path
from datetime import datetime

print("=" * 70)
print("МОДУЛЬ 4.5: МОДУЛІ ТА ПАКЕТИ В PYTHON")
print("=" * 70)

# ============================================================================
# 1. ЩО ТАКЕ МОДУЛЬ
# ============================================================================

print("\n" + "-" * 70)
print("1. ЩО ТАКЕ МОДУЛЬ?")
print("-" * 70)

print("""
Модуль - це Python файл (.py) який містить:
  - Функції
  - Класи
  - Змінні
  - Виконуваний код

Модулі дозволяють:
  ✓ Переносити код між проектами
  ✓ Організувати код логічно
  ✓ Уникати дублювання
  ✓ Захищати код (приватні функції)

Приклад структури:
  project/
  ├── main.py           (основна програма)
  ├── utils.py          (модуль з допоміжними функціями)
  ├── math_tools.py     (модуль для математики)
  └── data.json         (дані)
""")
print()

# ============================================================================
# 2. СПОСОБИ ІМПОРТУ
# ============================================================================

print("-" * 70)
print("2. СПОСОБИ ІМПОРТУ МОДУЛІВ")
print("-" * 70)

print("\nСпосіб 1: Імпорт всього модуля")
print("  import math")
print("  result = math.sqrt(16)  # Використовуємо через точку")
print(f"  Результат: {math.sqrt(16)}")
print()

print("Спосіб 2: Імпорт конкретної функції")
print("  from math import sqrt")
print("  result = sqrt(16)  # Використовуємо прямо")
print(f"  Результат: {sqrt(16)}")
print()

print("Спосіб 3: Імпорт з alias (псевдонім)")
print("  import math as m")
print("  result = m.pi  # Коротше писати")
print(f"  Результат: {math.pi}")
print()

print("Спосіб 4: Імпорт декількох за раз")
print("  from math import pi, sqrt, ceil")
print(f"  pi = {math.pi}, sqrt(25) = {sqrt(25)}")
print()

print("Спосіб 5: Імпорт всього (рідко використовується)")
print("  from math import *  # Імпортує все, не рекомендується")
print()

# ============================================================================
# 3. ВБУДОВАНІ МОДУЛІ PYTHON
# ============================================================================

print("-" * 70)
print("3. ПОПУЛЯРНІ ВБУДОВАНІ МОДУЛІ")
print("-" * 70)

print("""
Стандартна бібліотека Python:

Математика та числа:
  - math          - математичні функції
  - random        - випадкові числа
  - statistics    - статистичні функції
  - decimal       - точні десяткові числа

Дата та час:
  - datetime      - робота з датами
  - time          - виміри часу
  - calendar      - календар

Системні функції:
  - os            - операційна система (файли, дирекорії)
  - sys           - інтерпретатор Python
  - pathlib       - роботи з шляхами (сучасна)
  - shutil        - операції над файлами
  - tempfile      - тимчасові файли

Текст та данні:
  - re            - регулярні вирази
  - json          - JSON формат
  - csv           - CSV формат
  - string        - константи для рядків
  - textwrap      - форматування текста

Веб та мережа:
  - urllib        - завантаження з інтернету
  - http          - HTTP сервер
  - socket        - мережеві з'єднання

Інше:
  - collections   - спеціалізовані контейнери
  - itertools     - ітератори та комбінації
  - functools     - функціональне програмування
  - typing        - типізація
""")
print()

# ============================================================================
# 4. ОС ТА СИСТЕМНІ МОДУЛІ
# ============================================================================

print("-" * 70)
print("4. СИСТЕМНІ МОДУЛІ: os, sys, pathlib")
print("-" * 70)

# Модуль os
print("Модуль os - операційна система:")
print(f"  Система: {os.name}")  # nt (Windows) або posix (Linux/Mac)
print(f"  Поточна папка: {os.getcwd()}")
print(f"  Розділювач: '{os.sep}'")  # \ на Windows, / на Linux
print(f"  Переменні оточення: HOME={os.getenv('HOME', 'не встановлено')}")
print()

# Модуль sys
print("Модуль sys - інтерпретатор Python:")
print(f"  Версія Python: {sys.version.split()[0]}")
print(f"  Платформа: {sys.platform}")
print(f"  Розділювач шляхів: {sys.path[0][:50]}...")  # Першу 50 символів
print()

# Модуль pathlib
print("Модуль pathlib - сучасна робота зі шляхами:")
home = Path.home()
print(f"  Домашня папка: {home}")
desktop = home / "Desktop"
print(f"  Desktop: {desktop}")
print(f"  Існує? {desktop.exists()}")
print()

# ============================================================================
# 5. СТВОРЕННЯ ВЛАСНОГО МОДУЛЯ
# ============================================================================

print("-" * 70)
print("5. СТВОРЕННЯ ВЛАСНОГО МОДУЛЯ")
print("-" * 70)

print("""
Створимо модуль calculator.py:

# calculator.py
\"\"\"Модуль з математичними функціями\"\"\"

def add(a, b):
    \"\"\"Додає два числа\"\"\"
    return a + b

def multiply(a, b):
    \"\"\"Множить два числа\"\"\"
    return a * b

PI = 3.14159
VERSION = "1.0"

if __name__ == '__main__':
    print("Тестування calculator.py")
    print(add(5, 3))
    print(multiply(4, 5))

Використання:
  from calculator import add, multiply, PI
  result = add(10, 5)
  print(result)  # 15
  print(PI)      # 3.14159
""")
print()

# Створимо простий приклад модуля в пам'яті
print("Приклад: Створення простої функції як модуля")

def circle_area(radius):
    """Обчислює площу кола"""
    return math.pi * radius ** 2

def circle_circumference(radius):
    """Обчислює довжину кола"""
    return 2 * math.pi * radius

# Використання
area = circle_area(5)
circumference = circle_circumference(5)
print(f"  Площа кола (r=5): {area:.2f}")
print(f"  Довжина кола (r=5): {circumference:.2f}")
print()

# ============================================================================
# 6. ЩО ТАКЕ ПАКЕТ
# ============================================================================

print("-" * 70)
print("6. ЩО ТАКЕ ПАКЕТ?")
print("-" * 70)

print("""
Пакет - це папка з модулями та файлом __init__.py

Структура пакета:
  my_package/
  ├── __init__.py      (означає це пакет)
  ├── module1.py       (модуль 1)
  ├── module2.py       (модуль 2)
  └── subpackage/      (під-пакет)
      ├── __init__.py
      └── module3.py

Файл __init__.py:
  - Може бути пустим
  - Може містити конфігурацію пакета
  - Визначає що експортує пакет

Використання:
  from my_package import module1
  from my_package.module1 import function_name
  from my_package.subpackage import module3
""")
print()

# ============================================================================
# 7. __name__ == '__main__'
# ============================================================================

print("-" * 70)
print("7. ЗМІННА __name__")
print("-" * 70)

print(f"Поточне значення __name__: '{__name__}'")
print()

print("""
Змінна __name__ визначає де запущено модуль:
  - '__main__' - запущено як основна програма
  - 'module_name' - імпортовано з іншого модуля

Приклад:
  # calc.py
  def add(a, b):
      return a + b

  if __name__ == '__main__':
      # Цей код виконується тільки при прямому запуску
      print(add(5, 3))  # 8

  # Якщо в main.py: from calc import add
  # Код у if блоці не виконується!

Це дозволяє:
  ✓ Писати тести у тому ж файлі
  ✓ Використовувати файл і як модуль і як скрипт
  ✓ Правильно організувати код
""")
print()

# ============================================================================
# 8. PIP - МЕНЕДЖЕР ПАКЕТІВ
# ============================================================================

print("-" * 70)
print("8. PIP - МЕНЕДЖЕР ПАКЕТІВ PYTHON")
print("-" * 70)

print("""
pip - це інструмент для установки та управління пакетами.

Основні команди:

1. Установка пакету:
   pip install requests
   pip install flask==2.0.0  (конкретна версія)
   pip install 'requests>=2.0'  (мінімальна версія)

2. Видалення пакету:
   pip uninstall requests

3. Оновлення пакету:
   pip install --upgrade requests
   pip install -U requests

4. Перегляд установлених пакетів:
   pip list

5. Пошук пакету:
   pip search requests  (у деяких версіях недоступно)

6. Збереження залежностей:
   pip freeze > requirements.txt

7. Установка залежностей з файлу:
   pip install -r requirements.txt

8. Показати інформацію про пакет:
   pip show requests

Популярні пакети:
  - requests    - роботи з HTTP
  - flask       - веб-фреймворк
  - django      - великий веб-фреймворк
  - numpy       - наукові обчислення
  - pandas      - робота з даними
  - matplotlib  - графіки
  - pytest      - тестування
  - colorama    - кольорова консоль
""")
print()

# ============================================================================
# 9. ВІРТУАЛЬНЕ ОТОЧЕННЯ (venv)
# ============================================================================

print("-" * 70)
print("9. ВІРТУАЛЬНЕ ОТОЧЕННЯ (venv)")
print("-" * 70)

print("""
Віртуальне оточення - це ізольована копія Python для проекту.

Навіщо потрібно?
  ✓ Різні проекти мають різні версії пакетів
  ✓ Чистата система (пакети не в одній папці)
  ✓ Легше работать з командою

Як створити та використовувати:

1. Створення віртуального оточення:
   python -m venv venv
   python3 -m venv venv

2. Активація (залежить від ОС):

   На Windows:
   venv\\Scripts\\activate

   На Linux/Mac:
   source venv/bin/activate

   Ознака активації: префікс (venv) перед $

3. Установка пакетів:
   pip install requests
   pip install flask

   Пакети встановлюються в папку venv/

4. Збереження залежностей:
   pip freeze > requirements.txt

   Вміст requirements.txt:
   requests==2.28.1
   flask==2.2.0
   ...

5. На новому комп'ютері установити залежності:
   python -m venv venv
   source venv/bin/activate  (на Linux/Mac)
   pip install -r requirements.txt

6. Деактивація оточення:
   deactivate

Перевірка:
  which python     (на Linux/Mac)
  де python        (на Windows cmd)

  У активованому оточенні покаже venv/bin/python
  У деактивованому - /usr/bin/python (або інший шлях)
""")
print()

# ============================================================================
# 10. ПРАКТИЧНІ ПРИКЛАДИ
# ============================================================================

print("-" * 70)
print("10. ПРАКТИЧНІ ПРИКЛАДИ")
print("-" * 70)

# Приклад 1: Видання інформації про модуль
print("Приклад 1: Інформація про модуль math")
print(f"  Модуль: {math.__name__}")
print(f"  Файл: {math.__file__}")
print(f"  Функції: {[x for x in dir(math) if not x.startswith('_')][:5]}...")
print()

# Приклад 2: Документація функції
print("Приклад 2: Документація (docstring)")
print(f"  Функція: math.sqrt")
print(f"  Документація:")
print(f"  {math.sqrt.__doc__}")
print()

# Приклад 3: Перевірка що модуль містить
print("Приклад 3: Список експортованого з модуля")
from string import ascii_letters, digits, punctuation
print(f"  ASCII букви: {ascii_letters}")
print(f"  Цифри: {digits}")
print(f"  Знаки пунктуації: {punctuation}")
print()

# ============================================================================
# 11. ЗАВДАННЯ ДЛЯ САМОСТІЙНОЇ РОБОТИ
# ============================================================================

print("-" * 70)
print("11. ЗАВДАННЯ ДЛЯ САМОСТІЙНОЇ РОБОТИ")
print("-" * 70)

print("""
Завдання 1:
  Створіть файл math_utils.py з функціями:
  - circle_area(r)
  - rectangle_area(a, b)
  - triangle_area(a, b, c)

  Потім імпортуйте та використовуйте у основній програмі.

Завдання 2:
  Напишіть модуль string_utils.py з функціями:
  - reverse_string(s)
  - count_vowels(s)
  - is_palindrome(s)
  - remove_duplicates(s)

Завдання 3:
  Створіть структуру пакета:
  my_utils/
  ├── __init__.py
  ├── math_ops.py
  └── string_ops.py

  Імпортуйте функції обох модулів через пакет.

Завдання 4:
  Напишіть скрипт який:
  1. Створює віртуальне оточення
  2. Встановлює пакет requests
  3. Завантажує вміст сайту та виводить перші 100 символів

Завдання 5:
  Напишіть модуль config.py який містить:
  - Конфігурацію додатка (DEBUG, DATABASE_URL тощо)
  - Версію додатка

  Потім користуйте цей модуль в основній програмі.
""")
print()

# ============================================================================
# ИТОГИ
# ============================================================================

print("=" * 70)
print("ИТОГИ МОДУЛЮ")
print("=" * 70)

print("""
✅ Модулі та пакети:
  - Модуль - це Python файл (.py)
  - Пакет - папка з модулями та __init__.py
  - Імпортування розробляє доступ до модулів

✅ Способи імпорту:
  - import модуль
  - from модуль import функція
  - from модуль import функція as alias
  - import модуль as alias

✅ Вбудовані модулі:
  - math, random, datetime
  - os, sys, pathlib
  - json, csv, re
  - і багато інших

✅ Управління пакетами:
  - pip install (установка)
  - pip freeze (збереження залежностей)
  - pip install -r requirements.txt (установка залежностей)

✅ Віртуальне оточення:
  - python -m venv venv (створення)
  - source venv/bin/activate (активація)
  - deactivate (деактивація)

💡 Золоте правило:
  ✓ Завжди використовуйте віртуальне оточення!
  ✓ Зберігайте залежності у requirements.txt!
  ✓ Документуйте свої модулі!

🚀 Наступне: Практичні завдання та проекти
""")
